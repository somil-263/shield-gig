version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: shieldgig-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: shieldgig_core
    ports:
      - "5432:5432"
    restart: unless-stopped